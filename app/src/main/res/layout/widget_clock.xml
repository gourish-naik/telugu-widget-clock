<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/clock_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:padding="8dp">

    <!-- Optional rounded overlay behind the bitmap (use wrap_content so it follows the bitmap) -->
    <ImageView
        android:id="@+id/bg_overlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/rounded_overlay"
        android:visibility="gone"
        android:contentDescription="@null" />

    <!-- The rendered clock bitmap (wrap_content so it does not stretch) -->
    <ImageView
        android:id="@+id/clock_bitmap"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:scaleType="centerInside"
        android:contentDescription="@string/clock" />

</FrameLayout>
